{
  "name": "gamenight",
  "private": true,
  "workspaces": ["shared/protocol", "server", "web"],
  "scripts": {
    "dev": "npm run dev -w server",
    "build": "npm run build -w @gamenight/protocol && npm run build -w web && npm run build -w server",
    "lint": "npm run lint -w server",
    "typecheck": "npm run typecheck -w @gamenight/protocol && npm run typecheck -w server && npm run typecheck -w web"
  }
}
